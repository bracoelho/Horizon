| N-119 | **The facet probe's raw evidence was sitting untracked inside the PUBLIC FORK's clone** | radar-master | Tonight's pre-flight, which checks the fork tree is clean; the file was found by a check aimed at something else | Closed on sight | **CLOSED 2026-09-07 13:05 JST.** **WHAT:** `facet-probe-out.json`, 17.5 KB, the runs, agreement and usage of the $0.516 facet probe, written by my own experiment into `~/AI-Proj/Horizon` and never tracked. **TWO RISKS AT ONCE, in opposite directions: one `git add -A` in that clone publishes my experiment data into the public fork, and one `git clean` destroys the only evidence behind every number in `research/2026-09-07-facet-consistency-probe.md`** [fires: R5]. The write-up cited its figures and pointed at nothing. **MOVED to `research/data/2026-09-07-facet-probe-raw.json` and the write-up now names it.** **THE LESSON, and it is the receipt rule landing in a new place three hours after it was written: a finding whose evidence lives outside the repository that states the finding has no receipt**, whatever the prose says, and this one was additionally sitting where an unrelated routine command would have either leaked or deleted it. **THE CHECK IT NAMES: an experiment writes its output into the repo that will MASTER the finding, never into the clone it happened to run beside** |
