# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables
.env

# Data files (keep structure, ignore content)
data/config.json
data/seen.json
data/summaries/*.md
data/subscribers.json
data/mcp.secrets.json
data/mcp-secrets.json
data/mcp-runs/
data/x_cookies_*.json

docs/_posts/*.md
# Item pages are generated by a run and published straight to gh-pages,
the same as the digests above. Keeping them out of main stops a local
run from filling the source branch with a day's items.
docs/_items/*.md

# Logs
logs/*.log
*.log

# macOS
.DS_Store

# uv
.uv/

# todo
todo.txt
.ruff_cache
.pytest_cache
# data/commentary_proposal.json is deliberately tracked. The daily run writes
# it and the Draft Commentary dispatch reads it hours later, in a different
# job on a different machine, so it has to survive in the repository.
committed.txt
drafted_path.txt
drafted_title.txt
